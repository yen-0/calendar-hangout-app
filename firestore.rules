rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary permissive rules while public scheduling is being stabilized.
    // Replace this with proper per-collection authorization before production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
